# Standard Modules buil=in with python
# Some modules are built into the interpreter
# Some modules only work on certain OS systems

from sys import version as v, get_int_max_str_digits as it

print(v, '\n', it())